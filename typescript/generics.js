// Generic function identity<T>
function identity(value) {
    return value;
}
// Calling the function with different types
console.log(identity("Hello, TypeScript!")); // String
console.log(identity(42)); // Number
console.log(identity(true)); // Boolean
